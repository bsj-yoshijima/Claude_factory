# WP（Work Point）計測仕様

Claude Factory がゲーム内の「労働量」として使う WP の定義・根拠・限界。
実装は [otel.mjs](otel.mjs)（重みの定義は冒頭の `export const WP`）、表示は [metrics.html](metrics.html)。

---

## 0. これは何で、何ではないか

**WP は「活動量」の指標であり、「パフォーマンス」の指標ではない。**

- ✅ 目的: ゲームの燃料。一定量たまるとアイテムが製造される
- ✅ 使える: 稼働のリズムの可視化、同一人物の時系列の傾向
- ❌ 使えない: 人と人の比較、順位付け、評価

なぜ使えないかは [7. 妥当性の限界](#7-妥当性の限界) を必ず読むこと。ここを忘れると
「WPが高い人は生産性が高い」という誤った解釈に必ず流れる。

---

## 1. データソース

一次ソースは Claude Code の OpenTelemetry 出力のみ（`~/.claude/settings.json` の設定は
[README](README.md#-メトリクスotel-プロトタイプ) 参照）。使うのは **イベント1種 + メトリクス3種**。

| # | データ | 種類 | 用途 |
|---|---|---|---|
| 1 | `tool_result`（`success=true` のみ） | Event | `tool_name` ごとに重み加算 |
| 2 | `claude_code.lines_of_code.count` | Metric | `type=added` × 0.5 / `type=removed` × 0.3 |
| 3 | `claude_code.commit.count` | Metric | × 50 |
| 4 | `claude_code.pull_request.count` | Metric | × 150 |

### 採用理由

| データ | 理由 |
|---|---|
| `tool_result` | 「実際に何かをした」ことの唯一の直接証拠。離散イベントなので単調増加で数えられ、`success` があるので失敗を除外できる。5秒で届くので演出トリガにも使える |
| `lines_of_code.count` | 編集の「重さ」を反映する唯一の指標。これが無いと1行修正と200行リファクタが同価値になる |
| `commit.count` | 人間が「ここまでで一区切り」と判断した瞬間。意思を伴う数少ないシグナル |
| `pull_request.count` | 最も外部に成果として出る行為。頻度が低いのでレア素材の条件に向く |

### 不採用の理由（こちらのほうが設計上重要）

| 使わないデータ | 理由 |
|---|---|
| `token.usage` / `cost.usage` / `api_request` | 実労働と相関しない。冗長に出力するほど有利になる。実測では `cacheRead` が支配的（1セッション118M）で、巨大ファイルを1回読むだけで跳ねる。→ 演出専用の「電力メーター」に格下げ |
| `active_time.total` | 時間ベースは「開いて放置」で稼げる。ただし `type=user` は将来「人間の関与度」の補正係数に使える余地あり |
| `session.count` | 再起動連打で稼げる |
| `code_edit_tool.decision` | `tool_result` の `Edit` と**同じ行為が二重に届く**。両方入れると Edit が2倍になる |
| `hook_*` イベント | 自分の `settings.json` にフックを増やせば無限に稼げる。**絶対に入れない** |
| `user_prompt` / `assistant_response` | 「Claudeと雑談した量」に報酬が出てしまう |
| `mcp_server_connection` / `plugin_loaded` | 起動時の環境情報で労働ではない |

一貫した基準は **「自分で操作して増やせるものは入れない」**。

---

## 2. 重み

`otel.mjs` の `export const WP` が唯一の定義。ここを書き換えて再起動すれば
過去の生ログをリプレイして再集計される（[6. 重みを変える手順](#6-重みを変える手順)）。

| 対象 | 重み | 根拠 |
|---|---|---|
| `Edit` / `Write` / `NotebookEdit` | 10 | 成果物が変わる |
| `Bash` | 4 | 実行・検証。編集より軽く調査より重い |
| `Read` / `Grep` / `Glob` | 2 | 調査。安いが0にはしない |
| `Agent` / `Task` | 3 | **起動コストのみ。** 中身の労働は子の `tool_result` で数える |
| `Skill` | 25 | 実測で極端に低頻度（10日で `Bash` 8,080回 vs `Skill` 62回）。等倍だと存在感が消える |
| その他 / MCP ツール | 3 | 0にすると新ツールが無価値、高くすると水増しされる |
| `lines_of_code`(added) | × 0.5 | |
| `lines_of_code`(removed) | × 0.3 | 削除も労働だが、**追加と同額にすると「書いて消す」で無限に稼げる**ので非対称 |
| `commit.count` | × 50 | |
| `pull_request.count` | × 150 | |
| **サブエージェント内のツール実行** | 上記 **× 0.5** | [3](#3-サブエージェントの判定) 参照 |

### MCP ツールの制約

MCP ツールは個別名ではなく `tool_name="mcp_tool"` に丸められて届く。実際のツール名は
`tool_parameters`（例: `{"mcp_server_name":"ccd_session","mcp_tool_name":"mark_chapter"}`）の中。
**現状は全MCPツールが一律3WP。** ツール別に重みを変えるなら `tool_parameters` のパースが必要。

---

## 3. サブエージェントの判定

### 問題

実測で確認したこと: **子（サブエージェント）のツール実行は、親と同じ `session.id` の
`tool_result` として混ざって届く。** そして `tool_result` には `query_source` も
`agent.name` も**付いていない**（実測126件で確認）。

放置すると `Agent` の起動WPと子の労働WPが二階層で加算され、サブエージェントを
多用する人が構造的に有利になる（同一イベントの二重計上ではなく「二階層評価」）。

### 判定は2段構え

`Agent` には同期実行とバックグラウンド実行があり、挙動が全く違う。

| 実行形態 | `duration_ms` | 判定方法 |
|---|---|---|
| 同期（`run_in_background: false`） | 子の実行時間を含む（実測 28,600ms） | `[end - duration_ms, end]` の区間に入るか |
| **バックグラウンド（既定）** | **ほぼ0（実測 1〜42ms）** | 区間が作れないので、**直前に完了した `api_request` の `query_source`** で判定 |

バックグラウンド判定の根拠は「ツールは直前に完了した `api_request` の応答として実行される」
という因果。実測でも `agent:custom` の api_request 完了から 4〜20ms 後にツールが走っている。

`api_request.query_source` の実測値（**docs の `main`/`subagent`/`auxiliary` より詳細**）:

| 値 | 意味 |
|---|---|
| `sdk` | 親（メインループ） |
| `agent:builtin:general-purpose` | 組み込みサブエージェント |
| `agent:custom` | カスタムサブエージェント |
| `web_search_tool` | Web検索の内部呼び出し |
| `prompt_suggestion` | 入力補完の裏処理 |

判定効果（同じ生ログをリプレイして比較）:

| 判定 | 子と判定されたツール |
|---|---|
| `duration_ms` 区間のみ | 2件 |
| + `query_source` 判定 | **104件** |

### 既知の限界

**並列にバックグラウンドエージェントが多数走っている間は、親の作業と時間的に重なるため
判定は近似。** 実データにこういう箇所がある:

```
03:38:26.455  api  sdk (6713ms)      ← 親
03:38:26.525  TOOL Bash (90ms)       ← 親（正しく判定）
03:38:26.595  TOOL Grep (13ms)       ← 親と判定されるが、子の可能性あり
03:38:26.705  api  agent:custom
```

`tool_result` に `query_source` が無いのが根本原因なので、**完全な分離は OTel の
データだけでは不可能。**

### 実装上の注意

`Agent` の `tool_result` は**子より後に届く**（実測 seq18/27 → Agent seq33）ため、
受信時点では「子かどうか」が判定できない。よって **ツールWPは受信時に加算せず、
`tool_result` を1件ずつ保持して `snapshot()` で集計する。**

---

## 4. 1分あたりの上限（ガードレール）

```
WP.perMinuteCap = 200   // 1人1分あたり
```

1人 × 1分のバケットで生WPを合計し、上限を超えた分を切り捨てる。
メトリクス由来のWP（`lines_of_code` / `commit` / `pull_request`）も同じバケットに入れる。

**位置づけはバランス調整ではなくガードレール。** `Read` ループや大量並列エージェントの
ような病的なケースを止めるためのもの。属性の判定精度に依存せず頭を押さえられるので、
[3](#3-サブエージェントの判定) の近似の安全網になっている。

**クリップされた量は必ず画面に出す**（生WP / クリップ量 / 1分あたり最大 /
クリップされた分数）。黙って上限をかけると「全部数えた」ように見えてしまう。

### 較正の実測値

| 対象 | 1分あたりWP |
|---|---|
| ツールWPのみ（98分） | 中央値 20 / 75% 30 / 90% 45 / 95% 64 / 最大 83 |
| メトリクスWPも含む（101分） | 最大 **214.5**、上限200で **2分**クリップ（削減 0.6%） |

メトリクスWPを含めるとピークが上がるのは、`commit` の50WPと `lines_of_code` の分が
**メトリクスのエクスポート時刻に固まって届く**ため。10秒間隔にしているので塊は
実作業10秒分だが、コミットした瞬間の1分には集中する。

→ 上限200は「ほぼ効かない」ではなく **「たまに効く」水準**。余裕を持たせるなら300。

---

## 5. 較正データ（しきい値を決めるための実測）

### トランスクリプトからの概算（10〜40日分）

`~/.claude/projects/**/*.jsonl` のツール実行を数えた日次WP。
※ `Agent`=15、サブエージェント係数なし、上限なしの旧設定なので現行とは直接比較できない。

```
6月中旬     37 〜   600 WP  （軽い日）
7月上旬  1,500 〜 5,000 WP
7/21     7,049 WP
7/24    10,815 WP
7/27    13,746 WP  ← 最大
```

**日次の振れ幅が約370倍ある。** これが設計上いちばん重要な事実。
「しきい値N WPで1個完成」の固定設計では「1日1個」は絶対に成立しない。

10日分のツール実行回数:

```
Bash 8080 / Read 6329 / Edit 3563 / Grep 2378 / Agent 561
Glob 465 / Write 453 / Skill 62
```

### OTel 実測（現行の重み）

約1時間半で **生WP 2,946 / 有効WP 2,928**。
→ トランスクリプト概算の「1日5,000WP」よりペースが速い。**しきい値は要再較正。**

---

## 6. 重みを変える手順

受信した生ペイロードは `~/.claude/factory/otel-raw.jsonl` に全部残してある。

1. `otel.mjs` 冒頭の `export const WP` を書き換える
2. サーバを再起動する（`node server.mjs`）
3. 起動時に生ログを自動リプレイして再集計される

過去データで即座に結果が見えるので、重みの試行はこのループで回す。

---

## 7. 妥当性の限界

**WP は活動量の指標であり、パフォーマンスの指標ではない。**
パフォーマンスは `成果 ÷ 投入` だが、WPには分母がなく、しかも分子ではなく投入側を数えている。

| # | 問題 | 内容 |
|---|---|---|
| ① | **熟練度との逆相関** | 3回のツール実行で解決した人より、50回試行錯誤した人のWPが高い。構造的な逆相関 |
| ② | **難易度の補正なし** | 1行の難しいバグ修正 10WP、定型的な大量リネーム 数千WP |
| ③ | **質がゼロ評価** | 動くコードとバグだらけのコードが同じWP。さらに**バグを作って直すと2回分入る**（revert / hotfix が報酬になる） |
| ④ | **役割バイアス** | 設計・レビュー・議論中心の人はWPが出ない。会議・仕様検討は完全に不可視 |
| ⑤ | **観測範囲の偏り** | IDEで直接書く人は0WP。「Claude Codeを多用する人が有利」という結果しか出ない |
| ⑥ | **測ると壊れる（Goodhart）** | リーダーボードにした瞬間に指標としての意味を失う |

### 使ってよい範囲

| 用途 | 妥当性 |
|---|---|
| ゲームの燃料 | ✅ 本来の目的。パフォーマンス指標である必要がない |
| 稼働リズムの可視化 | ✅ 「工場が動いた/動いていない」は事実 |
| 同一人物の時系列傾向 | 🟡 習慣が一定なら読める。絶対値に意味はない |
| 人と人の比較・順位 | ❌ ①〜⑥が全部効く |

### 改善案: 投入と成果を2軸に分ける

現行の最大の設計問題は **投入と成果を1つの数字に混ぜていること**。

| 軸 | データ | 意味 |
|---|---|---|
| 活動WP | `tool_result` | どれだけ手を動かしたか（投入） |
| 成果WP | `commit` / `pull_request` / `lines_of_code` | 外形的に完了したもの（成果） |

分けると効率指標が作れる。

```
効率 = 成果WP / 活動WP   （PR1件あたり何回ツールを叩いたか。低いほうが良い）
```

片方だけでは必ずゲーム化されるが、両方を並べると水増しが見える。
ゲーム設計としても「活動WPでコンベアを進め、成果WPでレア素材が出る」と役割を分けられる。

リーダーボードをやるなら**順位ではなく「工場の稼働率」のような非競争的な表現**にする。

---

## 8. 未実装 / 残っている課題

| # | 項目 | 状態 |
|---|---|---|
| ① | サブエージェントの二階層加算 | ✅ 対処済（同期＋バックグラウンド両対応、ただし並列時は近似） |
| ② | **重複排除** | ❌ `event.sequence` / `tool_use_id` が届いているので実装可能。リトライで二重加算されうる |
| ③ | 1分あたり上限 | ✅ 対処済 |
| ④ | **日次バケット + 繰り越し** | ❌ アイテム製造ロジック本体。現状のWPは全期間の単一累計 |
| ⑤ | **製造しきい値の較正** | ❌ 1〜2日の実データを溜めてから決める必要がある |
| ⑥ | MCP ツール別の重み | ❌ `tool_parameters` のパースが必要 |
| ⑦ | 投入 / 成果の2軸分離 | ❌ [7](#7-妥当性の限界) の改善案 |

### 日次バケットの設計方針（決定済み・未実装）

日次の振れ幅が370倍あるため、**固定しきい値ではなく繰り越し式**にする。

```
1個 = N WP
超過WPは次のアイテムに繰り越す
```

- 大量に働いた日 → 2〜3個完成 + 繰り越し（頑張った実感が出る）
- 軽い日 → 0個だが翌日に持ち越されて完成する（無駄にならない）
- 長期平均が自動的に目標ペースに収束する

ハードキャップだと重い日の労働が消えて「損した感」が出るので繰り越しにする。
`N` は運用しながら中央値に追従させる（週次で再計算）。

---

## 9. 実測で判明した OTel 仕様の差異

WPロジックに直接影響したもの。詳細は [README](README.md#実測でわかったドキュメントとの差異)。

| 項目 | 実際 |
|---|---|
| イベント名 | `event.name` は `claude_code.` プレフィックス**無し**（`tool_result` 等）。docs 通りに書くとWPが全部0になる |
| 識別属性 | `user.email` / `session.id` / `organization.id` は resource ブロックではなく**各 datapoint / logRecord の attributes 側** |
| `tool_result` の親子情報 | `query_source` / `agent.name` が**付かない** |
| `api_request.query_source` | docs の3値より詳細（`sdk` / `agent:builtin:*` / `agent:custom` / `web_search_tool` / `prompt_suggestion`） |
| `event.timestamp` | 数値ではなく**ISO8601文字列**。時刻計算には `logRecord.timeUnixNano` を使う |
| `tool_name` (MCP) | 個別名ではなく `mcp_tool` に丸められる |
| `tool_parameters` | `OTEL_LOG_TOOL_DETAILS=0` でも届くが、中身はMCPの識別情報のみ（引数の内容は入らない） |
| プロンプト内容 | `prompt` / `response` 属性は存在するが値は `<REDACTED>` |
